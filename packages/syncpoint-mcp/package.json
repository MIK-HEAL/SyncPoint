{
  "name": "syncpoint-mcp",
  "version": "0.1.0",
  "description": "SyncPoint MCP Server — Model Context Protocol adapter for editor AI synchronization obligations",
  "type": "module",
  "main": "dist/main.js",
  "bin": {
    "syncpoint-mcp": "dist/main.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsx src/main.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "syncpoint-core": "workspace:*",
    "syncpoint-kernel": "workspace:*",
    "syncpoint-governance": "workspace:*",
    "syncpoint-context": "workspace:*",
    "syncpoint-adapters": "workspace:*",
    "syncpoint-server": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^20.14.0",
    "tsx": "^4.15.0"
  }
}
