这次演示是直接运行项目演示，并没有使用Agent通过MCP连接项目。只是展示底层协作协议的效果。如要使用请提前通过MCP连接相对应的agent

This demo runs the project directly and does not connect via an Agent through MCP. It is intended solely to showcase the effects of the underlying collaboration protocol. If you wish to use it with an agent, please ensure the corresponding agent is connected via MCP beforehand.