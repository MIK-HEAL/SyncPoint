● SyncPoint 代码仓库审查报告

  一、整体架构概览

  syncpoint-core (纯协议层，仅依赖 Zod)
      ↑
  syncpoint-server (应用层 + 数据库 + tRPC)
      ↑
      ├── syncpoint-cli (CLI 传输适配器)
      ├── syncpoint-mcp (MCP 传输适配器)
      ├── syncpoint-sdk (tRPC 客户端 SDK)
      │       ↑
      │       └── vscode-extension (编辑器集成)
      ├── syncpoint-plugin-code (代码域插件)
      └── syncpoint-plugin-generic-agent (通用代理插件)

  依赖方向严格单向，无循环依赖。 这是架构的最大亮点。

  ---
  二、代码边界评估

  2.1 包级边界 — 合理

  ┌─────────────────────┬───────────────────────────────────────────────────────────┐
  │        边界         │                           评价                            │
  ├─────────────────────┼───────────────────────────────────────────────────────────┤
  │ core ↔ server       │ 清晰：core 零 I/O，server 是所有 I/O 的唯一入口           │
  ├─────────────────────┼───────────────────────────────────────────────────────────┤
  │ server ↔ 传输适配器 │ 合理：CLI/MCP/SDK 都不包含业务逻辑，仅调用 application 层 │
  ├─────────────────────┼───────────────────────────────────────────────────────────┤
  │ core ↔ plugins      │ 良好：插件只依赖 core，无法访问 server 内部               │
  └─────────────────────┴───────────────────────────────────────────────────────────┘

  2.2 模块级边界 — 存在改进空间

  问题1：应用服务间紧耦合

  22 个服务直接互相导入，没有 mediator/事件总线层。例如：
  - loop-service.ts → 直接调用 sgCheckAgent, assembleProtocolGate, buildProjection
  - orchestration-service.ts → 直接调用 processOrchestrationEvent, sgCheckAgent

  这导致跨服务依赖难以追踪，新增服务时容易形成隐式依赖链。

  问题2：插件初始化问题已从 import 副作用收敛到显式 bootstrap

  旧的 `_plugin-init.ts` 副作用链已移除，当前通过显式 `bootstrap` 入口确保插件注册一致。
  剩余关注点不再是“import 顺序脆弱”，而是确保新服务接入时持续走统一 bootstrap 路径。

  问题3：syncpoint-core/src/index.ts 528 行

  barrel 导出文件过大，暴露了几乎所有内部模块。建议按子域拆分为多个入口（如 syncpoint-core/protocol,
  syncpoint-core/constraints 等）。

  ---
  三、过大的结构体分析

  3.1 超过15个字段的结构体

  ┌────────────────────────┬──────────────┬────────┬───────┐
  │         结构体         │     文件     │ 字段数 │ 风险  │
  ├────────────────────────┼──────────────┼────────┼───────┤
  │ project_memory（已拆分）│ schema.ts    │ 基表 15 │ � 中 │
  ├────────────────────────┼──────────────┼────────┼───────┤
  │ ContextSnapshotPayload │ models.ts    │ 19     │ 🟡 中 │
  ├────────────────────────┼──────────────┼────────┼───────┤
  │ wakeRequest (DB表)     │ schema.ts    │ 18     │ 🟡 中 │
  ├────────────────────────┼──────────────┼────────┼───────┤
  │ SyncGateSchema         │ sync-gate.ts │ 17     │ 🟡 中 │
  ├────────────────────────┼──────────────┼────────┼───────┤
  │ PeerContractSchema     │ models.ts    │ 14     │ 🟢 低 │
  └────────────────────────┴──────────────┴────────┴───────┘

  3.2 具体问题

  � project_memory — P2 已完成 breaking reset，但仍需继续观察基表膨胀

  原先 26 字段的单表膨胀问题已拆解为：
  - `project_memory` — 核心字段
  - `project_memory_tag` — tags
  - `project_memory_version` — fingerprint / supersede 链
  - `project_memory_projection` — projectionTarget
  - `project_memory_scope` — appliesTo 作用域
  - `project_memory_validation` / `project_memory_validation_action` — validator 配置
  - `project_memory_fts` — 全文检索

  当前结论：`project_memory` 已不再是“原始 JSON/CSV 业务字段堆叠的大表”，该项从“立即拆分”转为“保持 typed / normalized 方向，不再回退兼容层”。

  🟡 ContextSnapshotPayload (19字段) — 可接受但需关注

  这个结构体是快照的 payload
  部分，字段多是因为它需要完整描述一个工作上下文。目前是可接受的，但如果继续增长应考虑拆分为子对象（如 progressInfo,
  constraintInfo, contextInfo）。

  3.3 过大的文件

  ┌────────────────────────────────────────────────────────────┬──────┬───────────────────────────────┐
  │                            文件                            │ 行数 │             风险              │
  ├────────────────────────────────────────────────────────────┼──────┼───────────────────────────────┤
  │ syncpoint-mcp/src/tools.ts（聚合入口）                     │ 36   │ 🟢 低                         │
  ├────────────────────────────────────────────────────────────┼──────┼───────────────────────────────┤
  │ syncpoint-core/src/index.ts                                │ 528  │ 🟡 中                         │
  ├────────────────────────────────────────────────────────────┼──────┼───────────────────────────────┤
  │ syncpoint-server/src/application/context-policy-service.ts │ 569  │ 🟡 中                         │
  ├────────────────────────────────────────────────────────────┼──────┼───────────────────────────────┤
  │ syncpoint-server/src/application/loop-service.ts           │ 559  │ 🟡 中                         │
  ├────────────────────────────────────────────────────────────┼──────┼───────────────────────────────┤
  │ syncpoint-server/src/db.ts                                 │ 574  │ 🟡 中                         │
  ├────────────────────────────────────────────────────────────┼──────┼───────────────────────────────┤
  │ vscode-extension/src/extension.ts                          │ 703  │ 🟡 中                         │
  ├────────────────────────────────────────────────────────────┼──────┼───────────────────────────────┤
  │ syncpoint-server/src/schema.ts                             │ 509  │ 🟢 低（41张表，按表均分合理） │
  └────────────────────────────────────────────────────────────┴──────┴───────────────────────────────┘

  `syncpoint-mcp/src/tools.ts` 已完成按域拆分，当前是薄聚合入口。MCP 侧剩余重点从“拆文件”转为“保持域边界稳定，避免重新回到单文件膨胀”。

  ---
  四、流程架构可持续性评估

  4.1 优势

  ┌────────────────┬──────────────────────────────────────────────────────────────────────────────────┐
  │      特性      │                                       评价                                       │
  ├────────────────┼──────────────────────────────────────────────────────────────────────────────────┤
  │ 纯协议层       │ core 零 I/O 依赖，这是最关键的架构决策，保持得很好                               │
  ├────────────────┼──────────────────────────────────────────────────────────────────────────────────┤
  │ 状态机集中化   │ 所有状态转换逻辑集中在 states.ts, sync-gate.ts, wake.ts                          │
  ├────────────────┼──────────────────────────────────────────────────────────────────────────────────┤
  │ 一致的代码模式 │ 22 个 application 服务都遵循相同结构（input → validate → repo → event → result） │
  ├────────────────┼──────────────────────────────────────────────────────────────────────────────────┤
  │ 测试架构       │ __setDb() / __clear*ForTest() 模式让集成测试无需 mock 框架                       │
  ├────────────────┼──────────────────────────────────────────────────────────────────────────────────┤
  │ tRPC 路由极薄  │ 每个路由文件 <130 行，纯委托给 application 服务                                  │
  └────────────────┴──────────────────────────────────────────────────────────────────────────────────┘

  4.2 风险清单

  ┌──────────────────────────┬────────┬──────────────────────────────────────────────────────────────────────────────┐
  │           风险           │ 严重度 │                                     说明                                     │
  ├──────────────────────────┼────────┼──────────────────────────────────────────────────────────────────────────────┤
  │ resource-claim-service   │ 🔴 高  │ 资源声明是最核心的写控制机制，专项矩阵仍未补齐                               │
  │ 专项矩阵未补齐           │        │ 已有间接/混合覆盖，但仍缺 `rcClaim`/`rcRelease`/`rcList`/`rcDetectConflicts` 独立矩阵 │
  ├──────────────────────────┼────────┼──────────────────────────────────────────────────────────────────────────────┤
  │ sync-status-service      │ 🟢 低  │ 已有独立读模型测试；后续重点转为跟随协议演进维护覆盖                         │
  │ 已补测试                 │        │                                                                              │
  ├──────────────────────────┼────────┼──────────────────────────────────────────────────────────────────────────────┤
  │ checkpoint-review 与     │ 🟡 中  │ 两者生命周期几乎相同（create/approve/reject/cancel/status/list），缺共享抽象 │
  │ operation 服务模式重复   │        │                                                                              │
  ├──────────────────────────┼────────┼──────────────────────────────────────────────────────────────────────────────┤
  │ _scope-matchers.ts       │ 🟢 低  │ 当前是兼容 no-op 文件，待清理残余 import                                     │
  │ 兼容残留                 │        │                                                                              │
  ├──────────────────────────┼────────┼──────────────────────────────────────────────────────────────────────────────┤
  │ 架构文档需继续收束       │ 🟡 中  │ docs/ 已成形，但仍需补 ADR / 决策文档，避免实现与文档再次漂移                 │
  ├──────────────────────────┼────────┼──────────────────────────────────────────────────────────────────────────────┤
  │ 无接口抽象               │ 🟡 中  │ services 和 repositories 都是具体实现直接导入，无法替换实现                  │
  ├──────────────────────────┼────────┼──────────────────────────────────────────────────────────────────────────────┤
  │ 全局可变注册表           │ 🟢 低  │ Plugin 注册表使用全局 Map，测试可通过 clear* 重置，但并发场景未考虑          │
  └──────────────────────────┴────────┴──────────────────────────────────────────────────────────────────────────────┘

  4.3 关键判断：可持续吗？

  结论：可以持续发展，但需要偿还当前的结构债务。

  核心架构决策（纯协议层 + 单向依赖 + 传输适配器模式）是正确的，能够支撑长期迭代。但以下问题如果不处理，会在 6-12
  个月内成为瓶颈：

  1. resource-claim-service 缺专项矩阵 — 这是安全关键路径
  2. schema 中剩余 CSV / JSON 业务字段 —— 规范化清理尚未完成
  3. 服务间紧耦合 — 服务数量从 22 继续增长时，依赖图将难以追踪
  4. syncpoint-core barrel 过大 — 子域出口仍未分离

  ---
  五、优先级建议

  ┌────────┬───────────────────────────────────────────────────────────────┬──────────────────────────────┐
  │ 优先级 │                             行动                              │           预期收益           │
  ├────────┼───────────────────────────────────────────────────────────────┼──────────────────────────────┤
  │ P0     │ 为 resource-claim-service 补充测试                            │ 修复安全关键路径的测试盲区   │
  ├────────┼───────────────────────────────────────────────────────────────┼──────────────────────────────┤
  │ P0     │ 校准审查报告中的现状性结论                                    │ 执行清单可直接使用，不再混入旧快照 │
  ├────────┼───────────────────────────────────────────────────────────────┼──────────────────────────────┤
  │ P1     │ 清理其他 schema 的 CSV/JSON 业务字段                          │ 延续 normalized schema 方向  │
  ├────────┼───────────────────────────────────────────────────────────────┼──────────────────────────────┤
  │ P1     │ 提取 checkpoint-review / operation 共享生命周期抽象           │ 减少重复，统一协议演进       │
  ├────────┼───────────────────────────────────────────────────────────────┼──────────────────────────────┤
  │ P2     │ 持续统一 bootstrap 接入点                                     │ 防止服务新增时回退到隐式初始化 │
  ├────────┼───────────────────────────────────────────────────────────────┼──────────────────────────────┤
  │ P2     │ 补架构 ADR / 决策记录                                         │ 新开发者上手，设计意图传承   │
  ├────────┼───────────────────────────────────────────────────────────────┼──────────────────────────────┤
  │ P3     │ 将 context-policy-service.ts 的 prompt 格式化器拆分为独立模块 │ 单文件缩减，职责更清晰       │
  └────────┴───────────────────────────────────────────────────────────────┴──────────────────────────────┘

  六、可执行任务清单（按落地顺序）

  说明：
  - 以下清单以当前仓库状态和已确认的 breaking reset 方向为准，不机械复用上文所有审查结论。
  - `docs/` 已不为空，因此文档项调整为“补架构决策记录并同步现状”，不再作为“从 0 到 1 填目录”的任务。
  - `resource-claim-service` 已存在零散间接覆盖，因此目标是补齐专项测试矩阵，而不是只新增少量 happy-path 用例。
  - `project_memory`、`ContextSnapshot`、`CheckpointReview` 相关结构调整按 breaking reset 直接改模型，不做旧 DB 兼容、迁移脚本、双读双写。

  6.1 P0 — 立即执行

  - [ ] 为 `resource-claim-service` 建立专项测试矩阵
    - 范围：`rcClaim`、`rcRelease`、`rcList`、`rcDetectConflicts`
    - 场景：重叠资源、重复 claim、不同 `mode`、跨 agent 冲突、释放后冲突消失、与 `write-permit` / `file-permission-guard` 联动
    - 产出：独立测试文件 + 覆盖矩阵说明
    - 验收：安全关键路径具备正反向测试，不再主要依赖其他服务的间接覆盖

  - [x] 拆分 `packages/syncpoint-mcp/src/tools.ts`
    - 建议按域拆为：`resource`、`operation`、`sync-gate`、`project-memory`、`checkpoint-review`、`guard`、`playbook/review`
    - 入口文件只做注册与导出聚合，MCP tool name 对外保持不变
    - 验收：无单文件继续膨胀；新增工具只需改对应域模块

  - [x] 校准审查报告中的“现状性结论”
    - 核对已不成立项：`docs/` 空目录、`resource-claim-service` 零覆盖等
    - 将“问题快照”和“真实 backlog”分开，避免后续排期建立在过时前提上
    - 验收：`T.md` 或配套计划文档可以直接作为执行清单，不混入明显过时结论

  6.2 P1 — 紧随其后

  - [x] 消除 `_plugin-init.ts` 的 import 副作用初始化
    - 引入单一、显式的插件/bootstrap 入口
    - 确保单独导入 `loop-service`、`operation-service`、`constraint-evaluation-service` 等时行为一致
    - 验收：插件注册顺序不再依赖隐式 import 链，测试可显式验证初始化完成

  - [x] 为 `sync-status-service` 补聚合读模型测试
    - 覆盖阻塞器分类、快照拼装、约束可见性、资源/操作聚合视图
    - 验收：读模型逻辑具备独立回归保护，不再只靠端到端顺带覆盖

  - [x] 提取 `checkpoint-review` / `operation` 共享生命周期抽象
    - 共享 create/approve/reject/cancel/status/list 的通用骨架
    - 先抽状态流和 repo 协调，再保留领域差异
    - 验收：减少重复实现，后续协议扩展只需改一处主流程

  6.3 P2 — 结构债务清理

  - [x] 直接重写 `project_memory` 数据模型
    - 拆分核心内容、版本链、校验配置、投影配置
    - 同步补 `FTS`、索引、typed payload，去除业务上依赖原始 JSON/CSV 的字段
    - 验收：模型职责收敛，查询路径更清晰；不保留旧 schema 兼容层

  - [ ] 统一清理 schema 中的 CSV 列表字段和原始 JSON 业务字段
    - 优先处理资源、操作、审批、快照关联关系
    - 使用 join table 或 typed table 取代“逗号分隔 ID / JSON blob”
    - 验收：核心业务关系可被数据库结构直接表达和约束

  - [ ] 为服务边界建立更清晰的协作层
    - 目标不是一次性引入复杂事件总线，而是先减少 `loop`、`orchestration`、`sync-gate`、`projection` 之间的直接扇出
    - 可先抽 facade / coordinator，再评估是否需要事件化
    - 验收：核心流程调用链更短、更可追踪

  6.4 P3 — 可维护性收尾

  - [x] 拆分 `context-policy-service.ts` 中的 prompt formatter
  - [x] 拆分 `syncpoint-core/src/index.ts` 的 barrel 出口
  - [x] 补一份架构 ADR / tour，记录“显式初始化、schema breaking reset、MCP 工具分域”的设计决策

  6.5 建议执行顺序

  1. 先补 `resource-claim-service` 专项测试矩阵
  2. 再处理剩余 schema breaking reset（CSV / JSON 业务字段清理）
  3. 然后继续收敛核心服务协作层与 barrel 体积
  4. 最后补 ADR / 文档收口
